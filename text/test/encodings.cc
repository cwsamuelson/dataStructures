#include <text/encoding/ascii.hh>

#include <catch2/catch_all.hpp>

using namespace flp;

TEST_CASE("`format`::`encoding`::`ASCII`") {
}

TEST_CASE("`format`::`encoding`::`UTF-8`") {
}

TEST_CASE("`format`::`encoding`::`UTF-16`") {
}

TEST_CASE("`format`::`encoding`::`UTF-32`") {
}
