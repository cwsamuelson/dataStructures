#pragma once


namespace flp {

template<typename Key, typename Value>
struct SparseSet {
  // sparse set requires 2 data structures
  // real storage
  // something to map ID to effective index in real storage

  // it possibly requires a way of tracking free list?
};

} // namespace flp
