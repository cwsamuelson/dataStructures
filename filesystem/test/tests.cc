#include <fs/path.hh>
#include <fs/notifier.hh>

#include <catch2/catch_all.hpp>

using namespace flp;

TEST_CASE("`filesystem`::`path`") {
  fs::Path path;
}

TEST_CASE("`filesystem`::`notifier`") {
  fs::Path notifier;
}
